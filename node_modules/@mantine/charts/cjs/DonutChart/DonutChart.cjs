"use client";
const require_ChartTooltip = require("../ChartTooltip/ChartTooltip.cjs");
const require_DonutChart_module = require("./DonutChart.module.cjs");
let _mantine_core = require("@mantine/core");
let react_jsx_runtime = require("react/jsx-runtime");
let recharts = require("recharts");
//#region packages/@mantine/charts/src/DonutChart/DonutChart.tsx
const defaultProps = {
	withTooltip: true,
	withLabelsLine: true,
	paddingAngle: 0,
	thickness: 20,
	size: 160,
	strokeWidth: 1,
	startAngle: 0,
	endAngle: 360,
	labelsType: "value",
	tooltipDataSource: "all"
};
const varsResolver = (0, _mantine_core.createVarsResolver)((theme, { strokeColor, labelColor, withLabels, size }) => ({ root: {
	"--chart-stroke-color": strokeColor ? (0, _mantine_core.getThemeColor)(strokeColor, theme) : void 0,
	"--chart-labels-color": labelColor ? (0, _mantine_core.getThemeColor)(labelColor, theme) : void 0,
	"--chart-size": withLabels ? (0, _mantine_core.rem)(size + 80) : (0, _mantine_core.rem)(size)
} }));
const getLabelValue = (labelsType, value, percent, name, valueFormatter) => {
	if (labelsType === "name") return name;
	if (labelsType === "percent") return `${((percent || 0) * 100).toFixed(0)}%`;
	if (typeof valueFormatter === "function") return valueFormatter(value || 0);
	return value;
};
const getLabel = (labelsType, valueFormatter) => ({ x, y, cx, cy, percent, value, name }) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("text", {
	x,
	y,
	cx,
	cy,
	textAnchor: x > Number(cx) ? "start" : "end",
	fill: "var(--chart-labels-color, var(--mantine-color-dimmed))",
	fontFamily: "var(--mantine-font-family)",
	fontSize: 12,
	children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("tspan", {
		x,
		children: getLabelValue(labelsType, Number(value), Number(percent), name, valueFormatter)
	})
});
const DonutChart = (0, _mantine_core.factory)((_props) => {
	const props = (0, _mantine_core.useProps)("DonutChart", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, data, withTooltip, tooltipAnimationDuration, tooltipProps, pieProps, paddingAngle, withLabels, withLabelsLine, size, thickness, strokeWidth, startAngle, endAngle, tooltipDataSource, chartLabel, children, pieChartProps, valueFormatter, strokeColor, labelsType, attributes, cellProps, ...others } = props;
	const theme = (0, _mantine_core.useMantineTheme)();
	const getStyles = (0, _mantine_core.useStyles)({
		name: "DonutChart",
		classes: require_DonutChart_module.default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	const { resolvedClassNames, resolvedStyles } = (0, _mantine_core.useResolvedStylesApi)({
		classNames,
		styles,
		props
	});
	const pieData = data.map((item) => ({
		...item,
		fill: (0, _mantine_core.getThemeColor)(item.color, theme),
		stroke: "var(--chart-stroke-color, var(--mantine-color-body))",
		strokeWidth,
		...typeof cellProps === "function" ? cellProps(item) : cellProps
	}));
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_mantine_core.Box, {
		size,
		...getStyles("root"),
		...others,
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.ResponsiveContainer, { children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(recharts.PieChart, {
			...pieChartProps,
			children: [
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.Pie, {
					data: pieData,
					innerRadius: size / 2 - thickness,
					outerRadius: size / 2,
					dataKey: "value",
					isAnimationActive: false,
					paddingAngle,
					startAngle,
					endAngle,
					label: withLabels ? getLabel(labelsType || "value", valueFormatter) : false,
					labelLine: withLabelsLine ? {
						stroke: "var(--chart-label-color, var(--mantine-color-dimmed))",
						strokeWidth: 1
					} : false,
					...pieProps
				}),
				chartLabel && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("text", {
					x: "50%",
					y: "50%",
					textAnchor: "middle",
					dominantBaseline: "middle",
					...getStyles("label"),
					children: chartLabel
				}),
				withTooltip && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.Tooltip, {
					animationDuration: tooltipAnimationDuration,
					isAnimationActive: false,
					content: ({ payload }) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_ChartTooltip.ChartTooltip, {
						payload: data,
						classNames: resolvedClassNames,
						styles: resolvedStyles,
						type: "radial",
						segmentId: tooltipDataSource === "segment" ? payload?.[0]?.name : void 0,
						valueFormatter,
						attributes
					}),
					...tooltipProps
				}),
				children
			]
		}) })
	});
});
DonutChart.displayName = "@mantine/charts/DonutChart";
DonutChart.classes = require_DonutChart_module.default;
DonutChart.varsResolver = varsResolver;
//#endregion
exports.DonutChart = DonutChart;

//# sourceMappingURL=DonutChart.cjs.map