"use client";
const require_BarsList_module = require("./BarsList.module.cjs");
let _mantine_core = require("@mantine/core");
let react_jsx_runtime = require("react/jsx-runtime");
let react = require("react");
//#region packages/@mantine/charts/src/BarsList/BarsList.tsx
const defaultProps = {
	barGap: 5,
	minBarSize: 0,
	barHeight: 32
};
const varsResolver = (0, _mantine_core.createVarsResolver)((_theme, { barGap, minBarSize, barHeight }) => ({ root: {
	"--bars-list-gap": (0, _mantine_core.getSpacing)(barGap),
	"--bars-list-min-bar-size": (0, _mantine_core.rem)(minBarSize),
	"--bars-list-bar-height": (0, _mantine_core.rem)(barHeight)
} }));
const BarsList = (0, _mantine_core.factory)((_props) => {
	const props = (0, _mantine_core.useProps)("BarsList", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, attributes, data, valueFormatter, barsLabel, valueLabel, getBarProps, renderBar, barGap, minBarSize, barHeight, barColor, barTextColor, autoContrast, variant, ...others } = props;
	const theme = (0, _mantine_core.useMantineTheme)();
	const getStyles = (0, _mantine_core.useStyles)({
		name: "BarsList",
		classes: require_BarsList_module.default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	const maxValue = (0, react.useMemo)(() => Math.max(...data.map((item) => item.value), 0), [data]);
	const formatValue = (value) => {
		if (valueFormatter) return valueFormatter(value);
		return value.toString();
	};
	const bars = data.map((item, index) => {
		const percentage = maxValue > 0 ? item.value / maxValue * 100 : 0;
		const { style: barPropsStyle, className: barPropsClassName, ...restBarProps } = getBarProps ? getBarProps(item) : {};
		const itemBarColor = item.color || barColor || theme.primaryColor;
		const colors = theme.variantColorResolver({
			color: itemBarColor,
			theme,
			variant: item.variant || variant || "light",
			autoContrast
		});
		const backgroundColor = colors.background;
		const textColor = item.textColor || barTextColor ? (0, _mantine_core.getThemeColor)(item.textColor || barTextColor, theme) : colors.color;
		const defaultBar = /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			...getStyles("bar"),
			children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				...getStyles("barLabel", {
					className: barPropsClassName,
					style: {
						width: `${percentage}%`,
						backgroundColor,
						color: textColor,
						padding: percentage === 0 ? 0 : void 0,
						...barPropsStyle
					}
				}),
				...restBarProps,
				children: item.name
			}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				...getStyles("barValue"),
				children: formatValue(item.value)
			})]
		}, index);
		if (renderBar) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", { children: renderBar(item, defaultBar) }, index);
		return defaultBar;
	});
	const labelsRow = barsLabel || valueLabel ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		...getStyles("labelsRow"),
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", { children: barsLabel }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", { children: valueLabel })]
	}) : null;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(_mantine_core.Box, {
		...getStyles("root"),
		variant,
		...others,
		children: [labelsRow, bars]
	});
});
BarsList.displayName = "@mantine/charts/BarsList";
BarsList.classes = require_BarsList_module.default;
BarsList.varsResolver = varsResolver;
//#endregion
exports.BarsList = BarsList;

//# sourceMappingURL=BarsList.cjs.map