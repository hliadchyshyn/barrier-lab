"use client";
const require_ChartTooltip = require("../ChartTooltip/ChartTooltip.cjs");
const require_ChartLegend = require("../ChartLegend/ChartLegend.cjs");
const require_RadarChart_module = require("./RadarChart.module.cjs");
let _mantine_core = require("@mantine/core");
let react_jsx_runtime = require("react/jsx-runtime");
let react = require("react");
let recharts = require("recharts");
//#region packages/@mantine/charts/src/RadarChart/RadarChart.tsx
const defaultProps = {
	withPolarGrid: true,
	withPolarAngleAxis: true,
	withPolarRadiusAxis: false,
	withTooltip: false,
	withDots: false,
	tooltipAnimationDuration: 0
};
const varsResolver = (0, _mantine_core.createVarsResolver)((theme, { gridColor, textColor }) => ({ root: {
	"--chart-grid-color": gridColor ? (0, _mantine_core.getThemeColor)(gridColor, theme) : void 0,
	"--chart-text-color": textColor ? (0, _mantine_core.getThemeColor)(textColor, theme) : void 0
} }));
const RadarChart = (0, _mantine_core.factory)((_props) => {
	const props = (0, _mantine_core.useProps)("RadarChart", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, data, series, dataKey, gridColor, textColor, radarProps, radarChartProps, polarGridProps, polarAngleAxisProps, polarRadiusAxisProps, tooltipProps, withPolarGrid, withPolarAngleAxis, withPolarRadiusAxis, withTooltip, tooltipAnimationDuration, children, withLegend, withDots, dotProps, activeDotProps, legendProps, attributes, ...others } = props;
	const theme = (0, _mantine_core.useMantineTheme)();
	const getStyles = (0, _mantine_core.useStyles)({
		name: "RadarChart",
		classes: require_RadarChart_module.default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	const [highlightedArea, setHighlightedArea] = (0, react.useState)(null);
	const { resolvedClassNames, resolvedStyles } = (0, _mantine_core.useResolvedStylesApi)({
		classNames,
		styles,
		props
	});
	const radars = series.map((item, index) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.Radar, {
		name: item.name,
		dataKey: item.name,
		stroke: (0, _mantine_core.getThemeColor)(item.strokeColor || item.color, theme),
		fill: (0, _mantine_core.getThemeColor)(item.color, theme),
		fillOpacity: highlightedArea ? highlightedArea === item.name ? item.opacity || .4 : .05 : item.opacity || .4,
		dot: withDots ? {
			fillOpacity: 1,
			strokeOpacity: 0,
			strokeWidth: 1,
			fill: (0, _mantine_core.getThemeColor)(item.color, theme),
			stroke: (0, _mantine_core.getThemeColor)(item.color, theme),
			...dotProps
		} : false,
		activeDot: withDots ? {
			fill: (0, _mantine_core.getThemeColor)(item.color, theme),
			stroke: (0, _mantine_core.getThemeColor)(item.color, theme),
			...activeDotProps
		} : false,
		strokeOpacity: highlightedArea ? highlightedArea === item.name ? 1 : .1 : 1,
		isAnimationActive: false,
		...typeof radarProps === "function" ? radarProps(item) : radarProps
	}, index));
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_mantine_core.Box, {
		...getStyles("root"),
		...others,
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.ResponsiveContainer, {
			...getStyles("container"),
			children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(recharts.RadarChart, {
				data,
				...radarChartProps,
				children: [
					withPolarGrid && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.PolarGrid, {
						stroke: "var(--chart-grid-color)",
						...polarGridProps
					}),
					withPolarAngleAxis && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.PolarAngleAxis, {
						dataKey,
						...polarAngleAxisProps
					}),
					withPolarRadiusAxis && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.PolarRadiusAxis, {
						stroke: "var(--chart-grid-color)",
						...polarRadiusAxisProps
					}),
					withTooltip && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.Tooltip, {
						animationDuration: tooltipAnimationDuration,
						isAnimationActive: tooltipAnimationDuration !== 0,
						cursor: {
							stroke: "var(--chart-grid-color)",
							strokeWidth: 1
						},
						content: ({ label, payload, labelFormatter }) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_ChartTooltip.ChartTooltip, {
							label: labelFormatter && payload ? labelFormatter(label, payload) : label,
							payload,
							classNames: resolvedClassNames,
							styles: resolvedStyles,
							series,
							attributes
						}),
						...tooltipProps
					}),
					radars,
					withLegend && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(recharts.Legend, {
						verticalAlign: "bottom",
						content: (payload) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_ChartLegend.ChartLegend, {
							payload: payload.payload,
							onHighlight: setHighlightedArea,
							legendPosition: legendProps?.verticalAlign || "bottom",
							classNames: resolvedClassNames,
							styles: resolvedStyles,
							series,
							centered: true,
							attributes
						}),
						...legendProps
					}),
					children
				]
			})
		})
	});
});
RadarChart.displayName = "@mantine/charts/RadarChart";
RadarChart.classes = require_RadarChart_module.default;
RadarChart.varsResolver = varsResolver;
//#endregion
exports.RadarChart = RadarChart;

//# sourceMappingURL=RadarChart.cjs.map