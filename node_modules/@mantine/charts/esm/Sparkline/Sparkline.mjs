"use client";
import { AreaGradient } from "../AreaChart/AreaGradient.mjs";
import Sparkline_module_default from "./Sparkline.module.mjs";
import { Box, createVarsResolver, factory, getThemeColor, useProps, useStyles } from "@mantine/core";
import { jsx, jsxs } from "react/jsx-runtime";
import { useId, useMemo } from "react";
import { Area, AreaChart, ResponsiveContainer } from "recharts";
//#region packages/@mantine/charts/src/Sparkline/Sparkline.tsx
const defaultProps = {
	withGradient: true,
	connectNulls: true,
	fillOpacity: .6,
	strokeWidth: 2,
	curveType: "linear"
};
function getTrendColor(data, trendColors) {
	const first = data[0];
	const last = data[data.length - 1];
	if (first === null || last === null) return trendColors.neutral || trendColors.positive;
	if (first < last) return trendColors.positive;
	if (first > last) return trendColors.negative;
	return trendColors.neutral || trendColors.positive;
}
const varsResolver = createVarsResolver((theme, { color, data, trendColors }) => ({ root: { "--chart-color": trendColors ? getThemeColor(getTrendColor(data, trendColors), theme) : color ? getThemeColor(color, theme) : void 0 } }));
const Sparkline = factory((_props) => {
	const props = useProps("Sparkline", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, data, withGradient, fillOpacity, curveType, strokeWidth, trendColors, connectNulls, areaProps, attributes, ...others } = props;
	const getStyles = useStyles({
		name: "Sparkline",
		classes: Sparkline_module_default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	const id = useId();
	const mappedData = useMemo(() => data.map((value, index) => ({
		value,
		index
	})), [data]);
	return /* @__PURE__ */ jsx(Box, {
		...getStyles("root"),
		...others,
		dir: "ltr",
		children: /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(AreaChart, {
			data: mappedData,
			children: [/* @__PURE__ */ jsx(Area, {
				dataKey: "value",
				type: curveType,
				fill: `url(#${id})`,
				stroke: "var(--chart-color, var(--mantine-color-blue-filled))",
				isAnimationActive: false,
				connectNulls,
				strokeWidth,
				fillOpacity: 1,
				activeDot: false,
				...areaProps
			}), /* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsx(AreaGradient, {
				id,
				color: "var(--chart-color, var(--mantine-color-blue-filled))",
				fillOpacity,
				withGradient
			}) })]
		}) })
	});
});
Sparkline.displayName = "@mantine/charts/Sparkline";
Sparkline.classes = Sparkline_module_default;
Sparkline.varsResolver = varsResolver;
//#endregion
export { Sparkline };

//# sourceMappingURL=Sparkline.mjs.map