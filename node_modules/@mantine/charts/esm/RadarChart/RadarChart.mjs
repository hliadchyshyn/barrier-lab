"use client";
import { ChartTooltip } from "../ChartTooltip/ChartTooltip.mjs";
import { ChartLegend } from "../ChartLegend/ChartLegend.mjs";
import RadarChart_module_default from "./RadarChart.module.mjs";
import { Box, createVarsResolver, factory, getThemeColor, useMantineTheme, useProps, useResolvedStylesApi, useStyles } from "@mantine/core";
import { jsx, jsxs } from "react/jsx-runtime";
import { useState } from "react";
import { Legend, PolarAngleAxis, PolarGrid, PolarRadiusAxis, Radar, RadarChart, ResponsiveContainer, Tooltip as Tooltip$1 } from "recharts";
//#region packages/@mantine/charts/src/RadarChart/RadarChart.tsx
const defaultProps = {
	withPolarGrid: true,
	withPolarAngleAxis: true,
	withPolarRadiusAxis: false,
	withTooltip: false,
	withDots: false,
	tooltipAnimationDuration: 0
};
const varsResolver = createVarsResolver((theme, { gridColor, textColor }) => ({ root: {
	"--chart-grid-color": gridColor ? getThemeColor(gridColor, theme) : void 0,
	"--chart-text-color": textColor ? getThemeColor(textColor, theme) : void 0
} }));
const RadarChart$1 = factory((_props) => {
	const props = useProps("RadarChart", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, data, series, dataKey, gridColor, textColor, radarProps, radarChartProps, polarGridProps, polarAngleAxisProps, polarRadiusAxisProps, tooltipProps, withPolarGrid, withPolarAngleAxis, withPolarRadiusAxis, withTooltip, tooltipAnimationDuration, children, withLegend, withDots, dotProps, activeDotProps, legendProps, attributes, ...others } = props;
	const theme = useMantineTheme();
	const getStyles = useStyles({
		name: "RadarChart",
		classes: RadarChart_module_default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	const [highlightedArea, setHighlightedArea] = useState(null);
	const { resolvedClassNames, resolvedStyles } = useResolvedStylesApi({
		classNames,
		styles,
		props
	});
	const radars = series.map((item, index) => /* @__PURE__ */ jsx(Radar, {
		name: item.name,
		dataKey: item.name,
		stroke: getThemeColor(item.strokeColor || item.color, theme),
		fill: getThemeColor(item.color, theme),
		fillOpacity: highlightedArea ? highlightedArea === item.name ? item.opacity || .4 : .05 : item.opacity || .4,
		dot: withDots ? {
			fillOpacity: 1,
			strokeOpacity: 0,
			strokeWidth: 1,
			fill: getThemeColor(item.color, theme),
			stroke: getThemeColor(item.color, theme),
			...dotProps
		} : false,
		activeDot: withDots ? {
			fill: getThemeColor(item.color, theme),
			stroke: getThemeColor(item.color, theme),
			...activeDotProps
		} : false,
		strokeOpacity: highlightedArea ? highlightedArea === item.name ? 1 : .1 : 1,
		isAnimationActive: false,
		...typeof radarProps === "function" ? radarProps(item) : radarProps
	}, index));
	return /* @__PURE__ */ jsx(Box, {
		...getStyles("root"),
		...others,
		children: /* @__PURE__ */ jsx(ResponsiveContainer, {
			...getStyles("container"),
			children: /* @__PURE__ */ jsxs(RadarChart, {
				data,
				...radarChartProps,
				children: [
					withPolarGrid && /* @__PURE__ */ jsx(PolarGrid, {
						stroke: "var(--chart-grid-color)",
						...polarGridProps
					}),
					withPolarAngleAxis && /* @__PURE__ */ jsx(PolarAngleAxis, {
						dataKey,
						...polarAngleAxisProps
					}),
					withPolarRadiusAxis && /* @__PURE__ */ jsx(PolarRadiusAxis, {
						stroke: "var(--chart-grid-color)",
						...polarRadiusAxisProps
					}),
					withTooltip && /* @__PURE__ */ jsx(Tooltip$1, {
						animationDuration: tooltipAnimationDuration,
						isAnimationActive: tooltipAnimationDuration !== 0,
						cursor: {
							stroke: "var(--chart-grid-color)",
							strokeWidth: 1
						},
						content: ({ label, payload, labelFormatter }) => /* @__PURE__ */ jsx(ChartTooltip, {
							label: labelFormatter && payload ? labelFormatter(label, payload) : label,
							payload,
							classNames: resolvedClassNames,
							styles: resolvedStyles,
							series,
							attributes
						}),
						...tooltipProps
					}),
					radars,
					withLegend && /* @__PURE__ */ jsx(Legend, {
						verticalAlign: "bottom",
						content: (payload) => /* @__PURE__ */ jsx(ChartLegend, {
							payload: payload.payload,
							onHighlight: setHighlightedArea,
							legendPosition: legendProps?.verticalAlign || "bottom",
							classNames: resolvedClassNames,
							styles: resolvedStyles,
							series,
							centered: true,
							attributes
						}),
						...legendProps
					}),
					children
				]
			})
		})
	});
});
RadarChart$1.displayName = "@mantine/charts/RadarChart";
RadarChart$1.classes = RadarChart_module_default;
RadarChart$1.varsResolver = varsResolver;
//#endregion
export { RadarChart$1 as RadarChart };

//# sourceMappingURL=RadarChart.mjs.map