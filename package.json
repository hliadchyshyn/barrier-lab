{
  "name": "barrier-lab",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "dev:web": "pnpm --filter @barrier-lab/web dev",
    "dev:api": "pnpm --filter @barrier-lab/api dev",
    "build:web": "pnpm --filter @barrier-lab/web build",
    "test": "pnpm -r test"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
